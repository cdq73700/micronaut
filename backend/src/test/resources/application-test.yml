datasources:
  default:
    url: jdbc:postgresql://postgres:5432/postgres
    driverClassName: org.postgresql.Driver
    db-type: postgres
    username: postgres
    password: postgres
    dialect: POSTGRES
flyway:
  datasources:
    default:
      enabled: true